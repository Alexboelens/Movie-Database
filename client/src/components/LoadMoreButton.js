import React from 'react';
import { Button, Container, Row, Col } from 'reactstrap';

const LoadMoreButton = ({ array }) => {
    return (
        <Container className='text-center'>
            <Button>Load More..</Button>
        </Container>
    )
}

export default LoadMoreButton